import { Text } from "react-native";
import React from "react";

const ReusableTitle = ({ text }) => {
  return <Text>{text}</Text>;
};

export default ReusableTitle;

const styles = StyleSheet.create({});
